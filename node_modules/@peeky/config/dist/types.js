export class TestEnvironmentBase {
    envName;
    config;
    context;
    // eslint-disable-next-line no-useless-constructor
    constructor(envName, config, context) {
        this.envName = envName;
        this.config = config;
        this.context = context;
    }
}
