import type { AfterAllFn, AfterEachFn, BeforeAllFn, BeforeEachFn, Context, DescribeFn, TestFn } from '../types';
export declare function setupTestCollector(ctx: Context): {
    exposed: {
        describe: DescribeFn;
        test: TestFn;
        beforeAll: BeforeAllFn;
        afterAll: AfterAllFn;
        beforeEach: BeforeEachFn;
        afterEach: AfterEachFn;
    };
    collect: () => Promise<void>;
};
export declare type TestCollector = ReturnType<typeof setupTestCollector>;
