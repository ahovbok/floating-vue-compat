import { createRetry } from './test-flow.js';
import { createMockModule } from './mock.js';
export function createPeekyGlobal(ctx) {
    return {
        retry: createRetry(ctx),
        mockModule: createMockModule(ctx),
    };
}
