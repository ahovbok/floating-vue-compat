import { resolve, dirname } from 'pathe';
import { mockedModules } from '../mocked-files.js';
export function createMockModule(ctx) {
    return (path, stub) => {
        mockedModules.set(resolve(dirname(ctx.filename), path), stub);
    };
}
